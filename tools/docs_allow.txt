# Names this side's documentation may use that the code does not define.
# tools/check_docs.py reads this. This file is excluded from the source scan:
# it names every exempted symbol, so reading it as source would make the list
# evidence that its own symbols exist, and any name added here would go silent
# on every page. tools/test_check_docs.py asserts that exclusion.
#
# Two rules, both checks rather than conventions:
#
# 1. An entry is page-relative-path:name. RADIO_PRE_SYNC_US is a correct
#    historical quotation on radio/driver.md and was a stale claim in live
#    arithmetic on radio/timebase.md on the same day; a name-keyed list cannot
#    say that, and one line would silence the wrong occurrence. Five inherited
#    bare names were deleted on 2026-08-22 after removing them changed nothing:
#    they were the hub's algebra terms, exempting names this scope never uses.
# 2. The reason after # is required. A list that absorbs a real stale name is
#    worse than no list.

wl55_device/radio/driver.md:RADIO_PRE_SYNC_US  # named as the duplicate deleted in item 39
wl55_device/radio/driver.md:radio_air_time_us  # the general verb, named as what was split
wl55_device/radio/timebase.md:RADIO_FRAME_AIR_US  # named as a pre-rename name
wl55_device/radio/timebase.md:RADIO_POST_SYNC_AIR_US  # likewise
wl55_device/radio/timebase.md:RADIO_HUB_TX_DELAY_US  # named to say it does not exist yet
wl55_device/radio/driver.md:cmd_downlink  # the console command whose defect this records; removed 2026-08-22
wl55_device/radio/timebase.md:gap_plausible  # named as the screen that went with the console
wl55_device/radio/timebase.md:report_armed  # named as the RAM flag that no longer exists
wl55_device/testing/host-tests.md:link_v4  # a superseded vector set, named as superseded
wl55_device/radio/timebase.md:tx_gate  # named as the path removed with the console
wl55_device/radio/timebase.md:reserve_extend  # the 2026-08-21 account; removed by ADR-0023
wl55_device/security/replay.md:reserve_extend  # named as what used to sit in that gap
wl55_device/radio/pairing.md:reserve_extend  # the boundary table's historical row; removed by ADR-0023
wl55_device/radio/timebase.md:reserve_covers  # named in the 2026-08-21 analysis it belonged to
wl55_device/arch/README.md:store_reserve_counter  # named as the scheme ADR-0023 removed
wl55_device/radio/pairing.md:pair_init_key  # named in the past tense as pair_v3's seam, which ADR-0024 removed
wl55_device/radio/pairing.md:pair_init_key_from_z1  # same passage: the seam that existed to reach verify without a curve
wl55_device/radio/pairing.md:pair_init_test_seed_z1  # same passage: seeded the cached Z1 that no longer exists
wl55_device/radio/timebase.md:test_period  # a real test binary in the radio_stack submodule, outside this scan

# The SDR tools left OpenHub on 2026-08-25 for ~/.claude/skills/sdr/tools/, so
# nothing this checker can scan defines their names any more.
wl55_device/testing/README.md:rtl_sdr  # an external RTL-SDR binary, named in sdrdev.py in the sdr skill's tools
